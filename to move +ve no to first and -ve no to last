def rearrange(arr, n ) :
    i = 0
    for j in range(0, n) :
        if (arr[j] > 0) :
            temp = arr[j]
            arr[j] = arr[i]
            arr[i]= temp
            i = i + 1
    print(arr)
arr = [-5,6,3,-2]
n = len(arr)
rearrange(arr, n)
